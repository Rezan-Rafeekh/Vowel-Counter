# Vowel Counter using JavaScript
---

## 📄 Description

A JavaScript-based vowel analysis application that counts the **total number of vowels** as well as the **individual occurrences of each vowel (a, e, i, o, u)** in a user-entered string. The program is case-insensitive and provides results through multiple interactive buttons.

---

## ⚙️ Features

* Accepts a string input from the user
* Counts total number of vowels in the string
* Counts individual occurrences of:

  * a
  * e
  * i
  * o
  * u
* Case-insensitive vowel detection
* Uses separate buttons for each counting operation
* Displays results dynamically on the web page

---

## 🛠️ Technologies Used

* HTML5
* JavaScript (ES6)

---

## ▶️ How to Run

1. Open the project folder in **Visual Studio Code**
2. Open `index.html` in a web browser or using **Live Server**
3. Enter a string when prompted and use the buttons to view vowel counts

---

## 🎯 Learning Outcomes

* String traversal and character comparison
* Case-insensitive string processing
* Event-driven programming using buttons
* Dynamic DOM manipulation

---

## 📂 Project Files

* `index.html`
* `script.js`
